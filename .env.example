# ── MaMaison — Variables d'environnement (copier → .env) ─────
# Ne jamais committer ce fichier avec de vraies valeurs.

# SQL Server partagé (même mot de passe que momo_db et ALO)
DB_PASSWORD=SAME_AS_MOMO_DB_PASSWORD

# JWT — générer : openssl rand -base64 48
JWT_SECRET=une-cle-secrete-minimum-32-caracteres-a-changer
